import calculate.*;

class Demo{

    public static void main(String[] args){

        Add obj=new Add();
        Mul obj1=new Mul();
        System.out.println("add them: "+ obj.add(10,20)+" "+"mux them: "+obj1.mul(30, 60) );
    }
}
